import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rameshkumarsah.com.np',
  appName: 'rameshkumarsah.com.np',
  webDir: '.vercel/output/static/'
};

export default config;
